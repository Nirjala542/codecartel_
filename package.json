{
  "name": "skillproof-fullstack",
  "version": "1.0.0",
  "description": "SkillProof Monorepo Root",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
